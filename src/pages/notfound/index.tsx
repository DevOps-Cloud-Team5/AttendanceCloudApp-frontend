import { FC } from "react";
import Rootpage from "../root";

const NotFound: FC = () => {
    return (
        <Rootpage>
            <h1>404: Not found</h1>
        </Rootpage>
    );
};

export default NotFound;
