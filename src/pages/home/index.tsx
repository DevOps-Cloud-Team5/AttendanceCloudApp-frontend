import { FC } from "react";
import Rootpage from "../root";

const HomePage: FC = () => {
    return (
        <Rootpage>
            <h1>This is the Home Page!</h1>
        </Rootpage>
    );
};

export default HomePage;
